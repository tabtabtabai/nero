# `.agents/`

Local navigation and identity for this workspace (OpenClaw-style).

| File | Role |
|------|------|
| `SOUL.md` | Voice, values, and boundaries — read alongside `Agents.md` |
| `index.md` | This map |

Add short notes or skill snippets here when a subdirectory needs its own context; you can nest `.agents/` folders under `agents/<name>/` for focused workstreams.
